# 中文配對組字遊戲

> **Workspace:** /Users/yidanzhou/Documents/trae_projects/NCS-0904

---

**User**

const chars = ['日', '曆', '飛', '機', '太', '陽', '蘋', '果', '學', '校', '書', '本', 
               '電', '腦', '快', '樂', '大', '小', '上', '下', '水', '果', '光', '明',
               '文', '字', '音', '樂', '朋', '友', '天', '空', '花', '園', '汽', '車'];

// 預設詞組
const dictionary = new Set(['日曆', '飛機', '太陽', '蘋果', '學校', '書本', '電腦', '快樂', '文字', '音樂', '朋友', '天空', '花園', '汽車']);

let selectedCards = [];
const board = document.getElementById('game-board');

function initGame() {
    chars.forEach((char, index) => {
        const card = document.createElement('div');
        card.classList.add('card');
        card.innerText = char;
        card.dataset.index = index;
        card.addEventListener('click', () => selectCard(card));
        board.appendChild(card);
    });
}

function selectCard(card) {
    if (card.classList.contains('matched') || selectedCards.length >= 2) return;
    card.classList.add('selected');
    selectedCards.push(card);

    if (selectedCards.length === 2) {
        checkMatch();
    }
}

function checkMatch() {
    const [c1, c2] = selectedCards;
    const word1 = c1.innerText + c2.innerText;
    const word2 = c2.innerText + c1.innerText;

    // 驗證邏輯
    if (dictionary.has(word1) || dictionary.has(word2) || isReasonable(word1) || isReasonable(word2)) {
        c1.classList.add('matched');
        c2.classList.add('matched');
        document.getElementById('message').innerText = "太棒了！組成了：" + (dictionary.has(word1) ? word1 : word2);
    } else {
        setTimeout(() => {
            c1.classList.remove('selected');
            c2.classList.remove('selected');
        }, 500);
        document.getElementById('message').innerText = "這不是個合適的詞哦，再試試！";
    }
    selectedCards = [];
}

// 簡易邏輯擴展：如果你想加入動態判斷，可以在此擴充
function isReasonable(word) {
    const reasonable = ['陽光', '大樹', '文字']; // 額外接受的組合
    return reasonable.includes(word);
}

initGame();

生成遊戲

**Assistant**

目錄是空的，我來建立一個完整可玩的 HTML 遊戲檔案。

已完成 [index.html](file:///Users/yidanzhou/Documents/trae_projects/NCS-0904/index.html)，遊戲可直接在瀏覽器開啟遊玩。

內容包含：
- 你的完整 JavaScript 邏輯（36 張字卡、詞庫驗證、`isReasonable` 擴充）
- `game-board` 用 6×6 網格排列，手機自動切換為 4 欄
- `card`、`selected`（金黃色）、`matched`（綠色 + 彈跳動畫）三種樣式
- `message` 提示列，顯示組成的詞語或錯誤提示

直接雙擊檔案或執行 `open index.html` 即可開始遊戲。

---

**User**

可以幫我增加：1.每次訪問遊戲時字卡是亂序出現的。2.增加得分紀錄功能，答對一個加一分，共18分。3.在成功配對後立刻移除 selected 並加上 matched。4.當玩家把所有詞組都配對成功後，畫面需有提示，例如「恭喜通關！」）5.全部完成後，出現「再來一次」按鈕，重新開始遊戲，並且字卡必須是亂序的。

**Assistant**

好的，五項需求一起處理。有一個關鍵調整需要先說明：原本 36 張字卡中「大、小、上、下、水、果、光、明」這 8 張無法組成任何詞庫詞語（最多只能得 14 分），要達到「共 18 分」必須讓 36 張卡正好組成 18 個詞組，因此我會把「水果、大小、上下、光明」加入詞庫。

檔案在並行編輯時損毀了，我直接重寫完整檔案來修復。

檔案確實損毀了，現在重寫完整版本。

已完成，[index.html](file:///Users/yidanzhou/Documents/trae_projects/NCS-0904/index.html) 五項需求全部實作：

1. **亂序發牌**：新增 `shuffle()`（Fisher-Yates 洗牌演算法），每次載入頁面字卡順序都不同
2. **得分紀錄**：頂部顯示「得分：X / 18」，答對一詞加一分
3. **配對樣式**：成功時立刻移除 `selected` 並加上 `matched`
4. **通關提示**：集滿 18 分時顯示「恭喜通關！」
5. **再來一次**：通關後出現按鈕，點擊即重置分數並重新洗牌開局

重要調整說明：原詞庫只有 14 個詞，且「陽光」與「太陽」共用唯一的「陽」卡，實際最多只能得 14 分、永遠無法通關。因此：

- 詞庫新增 **水果、大小、上下、光明** 4 詞 → 18 個詞組正好用完 36 張字卡，必定可通關
- `isReasonable` 中的「陽光」會造成卡字衝突（用了它就凑不齊 18 分）、「大樹」缺少「樹」卡，故已清空該擴充列表並保留註解提醒

---

